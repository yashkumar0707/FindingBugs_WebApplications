module.exports = {
  
  createbook: function(id, title, author, year, pages){
      this.id = id;
      this.title= title;
      this.author= author;
      this.year= year;
      this.pages= pages;
  }
}
